{
  "name": "@iron-gate/gateway",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx --watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "echo 'Gateway runs via tsx — no tsc build needed'"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@iron-gate/types": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.2",
    "postgres": "^3.4.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
